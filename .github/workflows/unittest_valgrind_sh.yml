# Run all tests (in bash test framework) with 'force-enabled' valgrind.
#
# This workflow is run on 'self-hosted' runners.
name: Valgrind bash

on:
  workflow_dispatch:
  push:
  pull_request:
  schedule:
    - cron:  '0 18 * * 5'    # At 18:00 on Friday.

jobs:
  linux:
    name: valgrind
#    if: github.repository == 'pmem/pmdk'
    runs-on: ${{ matrix.os }}
    timeout-minutes: 4320 # wait max 3 days
    strategy:
      fail-fast: false
      matrix:
        os: [[self-hosted, rhel],[self-hosted, opensuse]]
        config: ['RUNTESTS -m force-enable obj_sync',
                 'RUNTESTS -p force-enable obj_sync',
                 'RUNTESTS -p force-enable obj_tx_alloc_mt',
                 'RUNTESTS -d force-enable obj_sync',
                 'RUNTESTS -e force-enable obj_sync']
    env:
      WORKDIR: utils/gha-runners

    steps:
      - name: Clone the git repo
        uses: actions/checkout@v3

      - name: Get system information
        run: ./$WORKDIR/get-system-info.sh

      - name: Build
        run: ./$WORKDIR/build-pmdk.sh

      - name: Create testconfig file
        run: ./$WORKDIR/create-testconfig.sh

      - name: Run tests
        run: cd src/test/ && ./${{ matrix.config }}

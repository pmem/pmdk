Persistent Memory Development Kit

This is src/test/util_is_poolset/README.

This directory contains a unit test for util_is_poolset().

The program in util_is_poolset.c takes a file name(s) as arguments:

For example:
	./util_is_poolset pool.set1 pool.set2

This will call util_is_poolset() on all files and print result value.
